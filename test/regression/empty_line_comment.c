#include "empty_line_comment.h"

/*
 * Defines a function that will remove the head node of the list
 * given as the first argument
 */

song_t *remove_song_from_head(song_t *to_remove) {
  song_t *head = to_remove;
} /* remove_song_from_head() */